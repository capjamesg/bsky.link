<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <title>Post by <% author.displayName %></title>

        <meta name="description" content="<%= data.text %>" />
        
        <meta property="og:title" content="Post by <%= author.displayName %>" />
        <meta property="og:description" content="<%= data.text %>" />
        <meta property="og:image" content="https://screenshot.jamesg.blog/?url=<%= url %>" />
        <meta property="article:author" content="<%= author.handle %>" />

        <link rel="icon" href="https://bsky.link/favicon.png" type="image/x-icon" />

        <% if (embed_type == "images") { %>
            <meta property="og:image" content="<%= embed[0].fullsize %>" />
        <% } %>

        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <main>
            <% if (parent) { %>
                <div class="h-entry">
                    <div class="author p-author h-card">
                        <img src="<%= parent.post.author.avatar %>" alt="<%= parent.post.author.displayName %>'s profile picture" height="50" width="50" class="u-photo">
                        <div class="author-name">
                            <h2 class="p-name"><%= parent.post.author.displayName %></h2>
                            <p class="p-nickname"><a href="https://staging.bsky.app/profile/<%= parent.post.author.handle %>" class="u-url"><%= parent.post.author.handle %></a></p>
                        </div>
                    </div>
                    <blockquote>
                        <span class="e-content"><%= parent.post.record.text %></span>
                    </blockquote>
                    <a href="<%= post_url %>" class="permalink u-url u-uid"><time class="dt-published datelink" datetime="<%= created_at %>"><%= created_at %></time></a>
                    <div class="post-actions">
                        ❤️ <%= parent.post.likeCount %> likes
                        🔄 <%= parent.post.repostCount %> reposts
                        💬 <%= parent.post.replyCount %> replies
                    </div>
                </div>
                <br>
            <% } %>
            <div class="h-entry <% if (parent) { %>indent<% } %>">
                <div class="author p-author h-card">
                    <img src="<%= author.avatar %>" alt="<%= author.displayName %>'s profile picture" height="50" width="50" class="u-photo">
                    <div class="author-name">
                        <h2 class="p-name"><%= author.displayName %></h2>
                        <p class="p-nickname"><a href="https://staging.bsky.app/profile/<%= author.handle %>" class="u-url"><%= author.handle %></a></p>
                    </div>
                </div>
                <blockquote>
                    <span class="e-content"><%= data.text %></span>
                    <% if (embed_type == "record") { %>
                        <div class="embed inner-embed">
                            <div class="author p-author h-card">
                                <img src="<%= embed.author.avatar %>" alt="<%= embed.author.displayName %>'s profile picture" height="50" width="50">
                                <div class="author-name">
                                    <h2><%= embed.author.displayName %></h2>
                                    <p class="p-nickname"><a href="https://staging.bsky.app/profile/<%= embed.author.handle %>" class="u-url p-name"><%= embed.author.handle %></a></p>
                                </div>
                            </div>
                            <blockquote>
                                <%= embed.value.text %>
                            </blockquote>
                        </div>
                    <% } else if (embed_type == "images") { %>
                        <% for (let i = 0; i < embed.length; i++) { %>
                            <img src="<%= embed[i].fullsize %>" alt="<%= embed[i].alt %>" class="embedded-image u-image" />
                        <% } %>
                    <% } %>
                </blockquote>
                <a href="<%= post_url %>" class="permalink u-url u-uid"><time class="dt-published datelink" datetime="<%= created_at %>"><%= created_at %></time></a>
                <div class="post-actions">
                    <a href="<%= post_url %>">❤️ <%= like_count %> likes</a>
                    <a href="<%= post_url %>">🔄 <%= repost_count %> reposts</a>
                    <a href="<%= post_url %>">💬 <%= reply_count %> replies</a>
                </div>
            </div>
            <% for (let r = 0; r < replies.length; r++) { %>
                <br />
                <div class="h-entry <% if (parent) { %>indent<% } %>">
                    <div class="author p-author h-card">
                        <img src="<%= replies[r].post.author.avatar %>" alt="<%= replies[r].post.author.displayName %>'s profile picture" height="50" width="50" class="u-photo">
                        <div class="author-name">
                            <h2 class="p-name"><%= replies[r].post.author.displayName %></h2>
                            <p class="p-nickname"><a href="https://staging.bsky.app/profile/<%= replies[r].post.author.handle %>" class="u-url"><%= replies[r].post.author.handle %></a></p>
                        </div>
                    </div>
                    <blockquote>
                        <span class="e-content"><%= replies[r].post.record.text %></span>
                    </blockquote>
                    <time class="dt-published datelink" datetime="<%= replies[r].post.record.createdAt %>"><%= replies[r].post.record.createdAt %></time>
                    <div class="post-actions">
                        ❤️ <%= replies[r].post.likeCount %> likes
                        🔄 <%= replies[r].post.repostCount %> reposts
                        💬 <%= replies[r].post.replyCount %> replies
                    </div>
                </div>
            <% } %>
        </main>
    </body>
</html>
